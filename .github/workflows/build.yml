name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  Test:
    name: Build and test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      # swiftly rather than setup-swift: it installs any toolchain swift.org has released, the
      # day it is released, so the pin here never waits on an action's version list.
      - uses: vapor/swiftly-action@v0.2
        with:
          toolchain: "6.4.0"
      # The encoder's integration test runs the real tools on a sample the tools make, and skips
      # itself where they are missing; installing them here is what stops it skipping. Avahi's
      # tools arrive with the node's discovery later and are installed now so the job's shape
      # does not change when it does.
      - name: Install ffmpeg and SQLite
        run: sudo apt-get update && sudo apt-get install -y ffmpeg libsqlite3-dev
      # On Linux deliberately: nothing below the server imports anything Apple-only, and this job
      # is what keeps that true.
      - name: Test
        run: swift test
